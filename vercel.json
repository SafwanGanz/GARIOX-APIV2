{
    "version": 2,
    "builds": [
        {
            "src": "./app.js",
            "use": "@vercel/node"
        }
    ],
    "config": {
             "framework": "express"
        },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}
